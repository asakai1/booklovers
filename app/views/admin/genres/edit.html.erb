<div class="container">
  <div class="row no-gutter">
    <div class="mx-auto mt-5">
        <%= flash[:notice] %>
        <!--ジャンルの編集-->
        <%= form_with model: @genre, url: admin_genre_path do |f| %>
            <%= f.text_field :name, class:"p-1 mr-3" %>
            <%= f.submit '更新する', class:"btn btn-outline-dark" %>
        <% end %>
    </div>
  </div>
</div>