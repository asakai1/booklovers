<div class="container">
  <div class="row">
    <div class="mx-auto mt-5">
      <p>おすすめの書籍について入力してください。</p>
      <p>書籍の写真は投稿できません。</p>
      <%= form_with model: @suggest_book do |f| %>
        <%= f.text_field :title, placeholder: "書籍名", autofocus: true %></br>
        <%= f.text_area :body, placeholder: "書籍の説明", class:"mt-3" %></br>
        <%= f.label :"ジャンル", class:"mt-3" %></br>
        <%= f.select :genre_id, options_from_collection_for_select(Genre.all, :id, :name) %></br>
        <%= f.submit '投稿する', class:"btn btn-outline-dark mt-3" %>
      <% end %>
    </div>
  </div>
</div>