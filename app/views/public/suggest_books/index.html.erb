<div class="container">
  <div class="row">
    <div class="mr-auto mt-5">
      <h4>ジャンルから探す</h4>
        <% @genres.each do |genre| %>
          <%= link_to genre.name, suggest_books_path(genre_id: genre.id), class:"btn btn-outline-dark p-2 m-1" %>
        <% end %>
      <h2 class="mt-5">おすすめ投稿一覧</h2>
      <p class="mt-3"><%= link_to "おすすめの書籍を投稿する", new_suggest_book_path %></p>
    <%= render 'public/users/index', books: @suggest_books %>
    </div>
  </div>
</div>